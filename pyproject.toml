[project]
name = "h2mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nnamllit1", email = "nothx" }
]
requires-python = ">=3.14"
dependencies = [
    "mcp>=1.26,<2",
]

[project.scripts]
h2mcp = "h2mcp:main"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
